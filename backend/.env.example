# Copiar a backend/.env y rellenar. config.py y src/phase1/extractor.py leen backend/.env.

# Obligatoria (Phase 1, gpt-4o-mini via LangChain). Sin ella el backend no arranca.
OPENAI_API_KEY=

# Opcionales (valores por defecto de config.py)
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_URL=http://localhost:3000
